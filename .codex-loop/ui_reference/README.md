# UI Reference

This folder stores visual references used for future diagrams, site graphics, and architecture cards.

Current status:

- one reference image has been identified from the current chat thread
- the binary image file is not yet stored locally because the current environment only exposes the image inside the conversation context
- a structured description is stored in `aiagent-architecture-reference.md`

If the original image file is later provided as a filesystem path, save it in this folder and keep the descriptive note beside it.

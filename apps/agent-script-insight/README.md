# Agent Script Insight

Interactive Claude Code command-script lab for the Learn LikeCode site.

## Local Commands

```bash
npm ci
npm run dev
npm run build
npm run lint
```

The Vite build uses relative asset paths, so the generated `dist/` output can be copied directly to `site/agent-script-insight/` for GitHub Pages.

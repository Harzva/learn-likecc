# evolution-2026-04-11-site-vibepaper-hero-cta-structure.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: 返回 VibePaper recurring task 做一轮入口层清理
- bounded target: 让 VibePaper hero 区的次按钮从旧的循环扩展入口切到当前更高价值的结构表入口

## Completed

- retargeted the secondary VibePaper hero CTA from `#loop` to `#four-questions`
- updated the CTA label so it now points readers toward the structural comparison entry rather than the later loop section
- updated the active loop plan to record this entrance-flow cleanup pass

## Failed or Deferred

- no browser render pass was run in this iteration
- no Markdown file was changed in this round because the improvement was hero-entry-only
- no Zhihu publication work was attempted because local time was still outside the allowed 08:00 to 23:00 window

## Decisions

- after the nav cleanup, the hero should reinforce the same reading order instead of pointing to an older secondary section
- future VibePaper entrance-flow passes should keep hero, nav, and section order aligned whenever the page grows

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-11-site-vibepaper-hero-cta-structure.md first, then choose exactly one best-next task from the pool by prioritizing main site work. The most likely next move is either another small VibePaper entrance-flow cleanup if one more navigation or CTA gap is visible, or a controlled return to Task 4 only if a new official changelog keyword adds a genuinely distinct teaching angle. Update the evolution note trail, prepare the next handoff, and after a successful iteration publish the site commit so GitHub Pages redeploys. If any branch reaches a Zhihu-ready state, prepare it but defer the publish step until local time is back inside 08:00 to 23:00.
```

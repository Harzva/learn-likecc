# evolution-2026-04-11-site-hermes-stack-gap-map.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: keep the overnight loop moving even though the queued Zhihu publish is time-blocked
- bounded target: use one bounded Hermes follow-up to tighten the “Hermes vs our stack” mapping instead of spending another late-night pass on already-verified publish prerequisites

## Completed

- treated the queued Superset Zhihu publish as time-blocked because the local time was still outside the allowed `08:00-23:00` window
- reopened the Hermes unpacked page for one narrow clarification pass on the existing unchecked mapping gap in the dedicated Hermes plan
- updated `site/md/topic-hermes-unpacked.md` and `site/topic-hermes-unpacked.html` with a new four-row gap map that makes `session seam`, `delivery path`, `memory / skills writeback`, and `execution contract` explicitly correspond to the nearest layers in our own `Claude Code / Like Code / codex-loop` stack
- used the new table to make the missing contracts concrete instead of leaving them as a high-level “we should be more explicit” conclusion

## Failed or Deferred

- did not resume the queued Zhihu publish because the local time remained outside the allowed publish window
- did not touch GitHub Pages or other site topics because this pass was intentionally limited to one Hermes clarification slice

## Decisions

- avoid another late-night media-prep loop once the Zhihu prerequisites are already proven; that would be lower value than a real site improvement
- keep the queued Zhihu publish as the next daytime media action, but let the overnight loop continue with bounded main-site work when the publish step is time-blocked
- keep the Hermes wave broadly locally done after this pass; this was a targeted reopen for one still-useful mapping gap, not a full new Hermes branch

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-11-site-hermes-stack-gap-map.md first. If the local time is inside the allowed Zhihu window, resume the queued media work by publishing /home/clashuser/hzh/item_bo/learn-likecc/wemedia/zhihu/articles/24-Superset解构外层调度-知乎图文.md with the multimodal pipeline first. If the local time is still outside the window, do not spend another pass on static publish-prep checks; instead choose exactly one new bounded main-site task from the remaining non-deferred pool. In either case, update the relevant dedicated plan, write one new evolution note, and publish the commit. GitHub Pages only needs checking if site-facing files changed.
```

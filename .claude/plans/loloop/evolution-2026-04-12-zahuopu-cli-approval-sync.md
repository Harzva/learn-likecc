# evolution-2026-04-12-zahuopu-cli-approval-sync.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: keep the overnight loop moving while Zhihu publication remains time-blocked
- bounded target: keep the deferred overnight media wave closed and finish one final Task 8 propagation pass so the root `AI杂货铺` entrance also reflects the current CLI shell-type plus approval-surface split

## Completed

- treated the queued Zhihu publish as still time-blocked because the local time remained outside the allowed `08:00-23:00` window
- kept the deferred overnight Zhihu-draft wave closed instead of reopening media work
- continued the same Task 8 propagation line because the root `site/topic-ai-zahuopu.html` shelf still flattened `CLI Agent` into a generic terminal-tools lane
- updated `site/topic-ai-zahuopu.html` and `site/md/topic-ai-zahuopu.md` so the root `CLI Agent` shelf now points readers toward:
  - shell type
  - approval surface
- added one explicit root-shelf note that the CLI lane should now be read as workflow-complete shell / TUI-first open shell / artifact-control CLI plus `action approval / artifact review / dangerous-command gate`
- added one matching reading-order cue so the root shelf now tells readers to judge shell type first and approval friction second
- recorded the Task 8 continuity in `active-reference-mining-topics-plan-v1.md` and the main site loop plan
- verified the site/html mirror still matches with `python3 tools/check_site_md_parity.py`

## Failed or Deferred

- did not reopen the leaf `topic-ai-cli-agent` page because the current gap was at the root-shelf entrance, not inside the leaf article
- did not reopen the `topic-ai-coding-tools` hub because that page already received the same approval-surface sync in the previous pass
- did not run a separate browser preview pass because this iteration was a bounded content-only site change and the repo already has automatic GitHub Pages deployment for `site/**` pushes

## Decisions

- treat this as the natural close-out of the current Task 8 CLI-taxonomy propagation wave because the distinction now exists at leaf, hub, and root-shelf levels
- defer further Task 8 propagation on this CLI approval thread unless a stronger cross-page taxonomy gap, new tool family, or stronger primary-source signal appears
- rely on the existing `.github/workflows/deploy.yml` path trigger to redeploy GitHub Pages once this site-facing commit is pushed

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-12-zahuopu-cli-approval-sync.md first. If the local time is inside the allowed Zhihu window, resume Task 1 by publishing /home/clashuser/hzh/item_bo/learn-likecc/wemedia/zhihu/articles/24-Superset解构外层调度-知乎图文.md with the multimodal pipeline first. If the local time is still outside the window, keep the deferred overnight Zhihu draft wave closed and treat the current Task 8 CLI approval-propagation thread as locally complete unless a stronger cross-page taxonomy gap or new primary-source signal appears; choose exactly one new bounded non-media slice from the remaining pool. Update the relevant plan, record one new evolution note, and publish the commit. GitHub Pages should redeploy automatically if the next pass again changes `site/**`.
```

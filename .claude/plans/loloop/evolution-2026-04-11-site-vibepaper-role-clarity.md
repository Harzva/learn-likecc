# evolution-2026-04-11-site-vibepaper-role-clarity.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: 持续推进 VibePaper recurring task
- bounded target: 在不增加新样本的前提下，补一个小型层级说明，把三主样本和 `AIDE ML` 候选底座的站内角色分清

## Completed

- added a compact role table to the VibePaper hub that explicitly separates the three main teaching samples from the `AIDE ML` upstream candidate substrate
- clarified why `Autoresearch` / `DeepScientist` / `AI Scientist-v2` remain the three shell anchors while `AIDE ML` stays in the comparison section
- updated the active loop plan to record the hierarchy-clarity cleanup pass

## Failed or Deferred

- no browser render pass was run in this iteration
- no new candidate repo was cloned in this iteration
- no Zhihu publication work was attempted because local time was still outside the allowed 08:00 to 23:00 window

## Decisions

- the next VibePaper passes should avoid blindly expanding the main sample table; new systems should either add a genuinely new shell shape or stay as controlled comparison candidates
- a small role table is sufficient for now because the current ambiguity was about page hierarchy rather than missing source evidence

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-11-site-vibepaper-role-clarity.md first, then choose exactly one best-next task from the pool by prioritizing main site work. The most likely next move is either one small VibePaper pass that adds a lightweight visual or comparison note around “主样本 vs 候选底座”, or another controlled intake only if the next system adds a clearly new shell shape and has equally strong primary sources. Update the evolution note trail, prepare the next handoff, and after a successful iteration publish the site commit so GitHub Pages redeploys. If any branch reaches a Zhihu-ready state, prepare it but defer the publish step until local time is back inside 08:00 to 23:00.
```

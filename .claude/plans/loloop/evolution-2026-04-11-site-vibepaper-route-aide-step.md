# evolution-2026-04-11-site-vibepaper-route-aide-step.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: VibePaper hub 继续做小范围阅读入口与教学路线打磨
- bounded target: 在 `VibePaper` 阅读路线里补一个显式的 `AIDE ML` 步骤，避免读者在 `AI Scientist-v2` 之后直接跳回站点

## Completed

- updated the route cards in `site/topic-vibepaper.html` to insert one explicit `AIDE ML` reading step between `AI Scientist-v2` and the final site-return step
- updated the matching route list in `site/md/topic-vibepaper.md` so the Markdown source now reflects the same five-step reading order
- updated the active loop plan to record this bounded Task 6 route-clarity pass

## Failed or Deferred

- no browser render pass was run in this iteration
- no new upstream repo intake was attempted in this round
- no Zhihu publication work was attempted in this iteration

## Decisions

- keep `AIDE ML` in the route as a candidate-lane teaching step rather than promoting it into the main sample heading
- prefer one extra reading step over silently leaving the candidate substrate implied, because the current hub now relies on that distinction

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-11-site-vibepaper-route-aide-step.md first, then choose exactly one best-next task from the pool by prioritizing main site work. The most likely next move is either another small VibePaper HTML/Markdown sync pass if one more wording or route gap is visible, or a controlled return to Task 4 only if a new official changelog keyword adds a genuinely distinct teaching angle. Since local time is inside 08:00 to 23:00, a queued Zhihu task can also be chosen in a future bounded iteration if it becomes the best next move. Update the evolution note trail, prepare the next handoff, and after a successful iteration publish the site commit so GitHub Pages redeploys.
```

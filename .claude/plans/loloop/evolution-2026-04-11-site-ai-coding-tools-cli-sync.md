# evolution-2026-04-11-site-ai-coding-tools-cli-sync.md

## Plan

- path: `.claude/plans/loloop/active-site-detail-polish-v1.md`
- milestone: site 细节打磨
- bounded target: 继续 Task 8，并把最新的 CLI shell-type 区分同步回 `topic-ai-coding-tools` 这张上层工具地图

## Completed

- chose `topic-ai-coding-tools` as the next bounded Task 8 destination after the `topic-ai-cli-agent` shell-type pass
- updated the top-level CLI Agent bucket so it now points readers toward the internal shell-type split instead of treating all terminal tools as one flat route
- synced the same clarification into the Markdown summary so the AI coding-tools page and CLI-Agent page no longer drift

## Failed or Deferred

- no browser render pass was run in this iteration
- no Zhihu adaptation pass was attempted in this iteration

## Decisions

- treat the coding-tools hub as the right follow-up surface because shell-type distinctions should flow back into the higher-level tool taxonomy
- keep the pass bounded to map / pick copy alignment instead of expanding the tool wall with another borderline case card

## Next Handoff

```text
Use codex-loop to continue the active plan at .claude/plans/loloop/active-site-detail-polish-v1.md. Read .claude/plans/loloop/evolution-2026-04-11-site-ai-coding-tools-cli-sync.md first, then continue Task 8 by reading .claude/plans/loloop/active-reference-mining-topics-plan-v1.md and choosing the next unchecked item from that dedicated plan. Stay on the reference-mining line because it remains the active loop task. The most likely next move is one bounded follow-up on the refreshed `topic-ai-coding-tools` destination, such as deciding whether this hub-level CLI sync is already enough for a local defer decision or whether one adjacent clarification is still justified. Update both plans as needed, record the next evolution note, and after a successful iteration publish the commit and check GitHub Pages because this pass changes site-facing files.
```

# Design/UI Claude Code Sample Map

Date: 2026-04-13  
Task: Task 16  
Status: bounded pass complete

## What changed

- Picked `topic-cc-unpacked-zh.html` as the first mature topic to test the `Design/UI` line against a real site artifact.
- Added a concrete `deck / slides / video` mapping section into the tutorial page.
- Added a structural sample-mapping section into the unpacked page so the topic compares not just tools, but media-shape decisions.

## Why this matters

- The `Design/UI` line now has a reusable first sample instead of staying at the level of abstract tool comparison.
- `Claude Code` is a strong seed topic because it already has clear section boundaries, layered system explanation, and enough visual structure to support deck, slide, and motion variants.

## Next handoff

- Keep using `.claude/plans/loloop/active-design-ui-topic-plan-v1.md`.
- The next bounded pass should map the first plugin-facing line:
  - `Mermaid`
  - `html-card-to-png`
  - `webpage-screenshot-md`
- The goal is to define when a mature topic should choose `diagram / screenshot / deck / slides / video` as its next expression layer.

# v2.0.4_plan.md - 尝试运行与运行时修复

## 前倾回顾

### v2.0.3 完成情况

| 成果 | 状态 |
|------|------|
| 创建 30+ 缺失模块 stub | ✅ |
| 编译错误从 2034 降至 ~2137 | ⚠️ (有波动) |
| Git 仓库公开推送 | ✅ |

### 当前编译错误: 2137

### 主要错误分析

| 错误类型 | 数量 | 说明 |
|----------|------|------|
| SDK 类型不匹配 | ~50 | QueryEngine.ts 中 SDK 类型问题 |
| 缺失属性 (content, stop_reason) | ~30 | 消息类型属性问题 |
| diffArrays 导出缺失 | 1 | diff 包类型声明 |

### v2.0.3 结论

虽然创建了所有计划的 stub 模块，但错误数有轻微波动。
参考 claude-code2 项目说明：**tsc 错误不阻止 Bun 运行时执行**

---

## v2.0.4 目标

**目标**: 尝试运行 `bun run dev`，修复运行时错误

---

## 任务 1: 检查运行环境

```bash
# 检查 Bun 是否安装
bun --version

# 如果未安装，安装 Bun
curl -fsSL https://bun.sh/install | bash
```

---

## 任务 2: 安装依赖并运行

```bash
cd ccsource/like-code-main
bun install
bun run dev
```

---

## 任务 3: 修复运行时错误

根据运行时错误输出，逐个修复：

1. 缺失的运行时依赖
2. 运行时类型错误
3. 模块加载问题

---

## 任务 4: 更新 README

创建项目 README.md，包含：

- 项目状态
- 编译进度
- 长期计划
- 使用说明

---

## 执行命令

完成本计划后，执行：

```bash
/loop 30min 完成所有的v2.0.4_plan.md
```

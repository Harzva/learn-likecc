# v1.0.2_plan.md - 多窗口 Tab 模式起步版

## 目标

`v1.0.2-likecode` 先不追求完整分屏，而是把“同一个 session 下多个任务 tab”打成第一版可用能力。

这一版聚焦：

- tab 状态模型
- tab UI
- tab 切换与管理
- tab 级 transcript / model / provider 绑定
- subagent 面板占位
- `/show:slash`
- 启动爱心 logo 与 Like Code 头图

---

## 为什么是这一版

当前已经完成：

- 多 Provider 第一阶段
- `/model` 外部模型展示
- `/show` 配置查看
- 启动界面配置可见性
- 多窗口 / subagent 视图设计稿 V1

所以 `v1.0.2-likecode` 最合适的下一步，不是直接冲 pane，而是把 tab 模式落地，形成一个真正可交付、可演示、可继续扩展的版本。

同时，这一版也已经验证出一个重要判断：

- `tab` 和 `branch` 不重复
- 但当前轻量 tab 只是壳层
- 真正高价值的下一步应是 `panel / 分屏 / subagent 状态视图`

---

## 版本范围

### In Scope

- [x] 为 session 增加 tab 状态模型
- [x] 顶部增加 tab 行
- [x] 支持新建、切换、关闭、重命名 tab
- [x] 让 transcript 与 tab 开始绑定
- [x] 让当前 model / provider 与 tab 绑定
- [x] 为 subagent 增加可折叠状态面板第一版
- [x] 让 pane 模式下底部出现多个可见对话框区域
- [x] 增加 `/show:slash`
- [x] 启动 logo 改为爱心形态并继续收成蓝色版本
- [x] 启动 logo 收成蓝色爱心 + 多行大字 `Like`
- [x] README / roadmap / CHANGELOG / Release 说明同步

### Out of Scope

- [ ] 真正的 pane 分屏
- [ ] 拖拽布局
- [ ] 多列日志墙
- [ ] 完整 subagent 实时嵌入视图

---

## 任务拆分

### 任务 1：状态层

- [x] 新增 `activeTabId`
- [x] 新增 `tabOrder`
- [x] 新增 `tabs`
- [x] 新增 `sessionTabs` / `SessionTabState` / `SessionTabsMetadata` 基础模型
- [ ] 兼容旧 session 自动回退单窗口模式

### 任务 2：顶部 Tab UI

- [x] 渲染 tab 行
- [x] 高亮当前 tab
- [x] 展示标题、模型、provider、状态
- [x] 增加 `+` 新建入口

### 任务 3：Tab 动作

- [x] 新建 tab
- [x] 顺序切换 tab
- [x] 编号切换 tab
- [x] 重命名 tab
- [x] 关闭 tab

### 任务 4：状态绑定

- [x] transcript 与 tab 开始绑定
- [x] todo snapshot 与 tab 开始绑定
- [x] model/provider 与 tab 绑定
- [x] repo/worktree 与 tab 绑定
- [x] active pane 的 draft 输入随 tab 保存和恢复
- [x] pane 的 transcript 预览随 tab 保存和恢复
- [x] 主消息区会随 tab 切换到各自保存的 transcript
- [x] todo lane 快照会随 tab 保存和恢复
- [x] non-active pane 显示 transcript 摘要、todo 摘要、draft 提示和更明确的激活提示
- [ ] transcript 继续做真正隔离，而不只是先完成 UI 层切换
- [ ] todo lane 继续做真正隔离，而不只是先完成 UI 层快照恢复
- [x] 为每个 tab 固定逻辑 todo lane 标识，避免 pane 长期都挂同一个默认 lane 文案
- [x] 让 tasks 链开始读取 active pane 的逻辑 lane override，给后续真隔离铺底
- [ ] 多个 pane 继续升级成真正的多活跃输入，而不只是当前 active pane 使用真实输入框

### 任务 5：快捷键

- [x] 接入 `Ctrl+g` 前缀模式
- [x] 接入 `c / n / p / x / s`
- [x] 接入 `1-9` 快速跳转
- [x] 接入 `h / l` 左右 pane 聚焦
- [x] 增加 `/tab focus left|right|1|2`
- [ ] 评估 `Alt+1-9` 快切

### 任务 6：Subagent 面板占位

- [x] 支持折叠 / 展开
- [x] 支持展示 agent 名称、任务、状态与当前聚焦 transcript
- [x] 预留后续真实数据接入口
- [ ] 继续补仓库、目录、更多 live 运行细节

### 任务 7：命令可见性与品牌细节

- [x] 增加 `/show:slash`
- [x] 在终端里输出 slash command 用法摘要
- [x] 启动 logo 改为红色爱心
- [x] 将 panel / 分屏提升为下一优先级

### 任务 8：发布与验证

- [ ] 回归验证单窗口模式
- [x] 验证 tab 与 `/model` 共存
- [ ] 验证 tab 与 `/show` 共存
- [x] 验证 `/show:slash` 输出完整可读
- [x] 更新 README
- [x] 更新 CHANGELOG
- [ ] 准备 Release 文案
- [ ] 检查 GitHub Pages 文档是否需要同步
- [x] 并行产出 localhost Web UI 工作台设计稿，为 CLI / Web UI 分工提供决策依据
- [x] 并行产出 localhost Web UI 读取协议草案，为后续接口实现铺底
- [x] 起好 localhost 第一版只读接口骨架：`session / pane / transcript / events`
- [x] 让 localhost 根路径直接渲染 dashboard，而不只是返回 API 索引 JSON
- [x] 重做 localhost Web 工作台视觉层，让它从开发态 JSON 面板升级成更像正式工作台的多栏结构
- [x] 增加 `/api/sessions/current/subagents`，明确当前 subagent 摘要来自 `AppState.tasks` + pane 绑定
- [x] 让 transcript 摘要开始抽取 `text / thinking / tool_use / tool_result`
- [x] 让 Web UI transcript 开始输出 `cards / workflow` 两层结构，方便前端同时展示消息流、thinking 卡片和工具时间线
- [x] 让 pane 初始化进入时也优先恢复该 pane 自己的 todo 快照
- [x] 让 Web UI transcript 继续输出 `stages`，开始按 `prompt / thinking / tool / response / system` 展示阶段节点
- [x] 让 tool 卡片补 `input / output / toolUseId`
- [x] 让新建 pane/tab 以空 todo 快照起步，减少跨 pane 的全局残留状态
- [x] 让 pane 开始保存和恢复 `inputMode / pastedContents / stashedPrompt`
- [x] 让 Web UI transcript 补出 `turns / toolPairs`，开始支持更清楚的 tool 配对和 turn replay
- [x] 让 Web UI 首页直接渲染 `Tool Pairs`
- [x] 让启动头的 `Like Code` 视觉统一成蓝色品牌头图
- [x] 让 Web UI transcript 继续补出 `toolChains`，并给 `toolPairs` 增加 `chainId / step / previous / next`
- [x] 让 `progress` 消息也开始挂到对应的 tool 链上，增强真实工具执行中的中间过程可见性
- [x] 继续把 `vimMode / history search / bashes dialog / help / message selector` 这些输入相关开关往 pane 内部迁
- [x] 让 `message selector` 的预选消息也跟随 pane 保存和恢复
- [x] 让 pane 的 `conversationId / submitCount` 也开始跟随窗口保存和恢复
- [x] 让真实 `tool progress` 也开始进入 `toolChains`，在最终 `tool_result` 返回前先展示执行中的工具链
- [x] 让 transcript 的 `show all / dump mode` 也开始跟随 pane 保存和恢复
- [x] 让 transcript 搜索词也开始跟随 pane 保存和恢复
- [x] 让 transcript 搜索条的打开状态也开始跟随 pane 保存和恢复
- [x] 让 transcript 搜索的命中数量与当前位置也开始跟随 pane 保存和恢复
- [x] 让退出 transcript 后的 pane 也尽量保留搜索上下文，重新进入后更接近继续上次阅读定位
- [x] 让 `toolChains.steps` 开始明确拆成 `tool_use / progress / tool_result`
- [x] 固定三套源码分层：原始源码快照、可运行重建基线、Like Code 主开发线
- [x] 为 Like Code 主开发线补出 `ccsource/like-code-main` 正式入口，并保留 `ccsource/claude-code-main` 兼容旧路径
- [x] 将关键入口页面、构建命令与部分计划文档的主开发线路径统一成 `ccsource/like-code-main`
- [ ] 继续把 coding 过程提取得更细，给 Web UI 流程图与工作流展示使用

---

## 交付标准

### 必须达成

- 用户能在一个 session 下看到多个 tab
- 用户能切换 tab 且不丢失各自 transcript
- 用户能看清每个 tab 的模型 / provider / 状态
- 文档、计划、CHANGELOG、Release 说明同步

### 可选增强

- [ ] tab 未读计数
- [ ] tab 类型图标
- [ ] tab 快速选择器

---

## 关联文档

- `./multi-window-subagent-design-v1.md`
- `./likecode-model-freedom-roadmap.md`
- `./localhost-web-ui-design-v1.md`
- `./localhost-web-ui-protocol-v1.md`

# v2.0.6_plan.md - API 配置与完整功能测试

## 前倾回顾

### v2.0.5 完成情况

| 成果 | 状态 |
|------|------|
| 测试 --version 命令 | ✅ 输出正确 |
| 测试 --help 命令 | ✅ 显示完整 CLI 选项 |
| 测试管道模式 | ⚠️ 需要 API key |

### 当前状态

| 指标 | 值 |
|------|------|
| 编译错误 | 2175 |
| 运行状态 | ✅ CLI 正常 |
| 帮助/版本 | ✅ 正常 |
| API 调用 | ⚠️ 需要配置 |

### v2.0.5 分析

程序核心功能正常，CLI 解析完整。管道模式卡住是因为：
1. 程序等待 API 响应
2. 需要 ANTHROPIC_API_KEY 环境变量

---

## v2.0.6 目标

**目标**: 配置 API 并完成完整功能测试

---

## 任务 1: 检查 API 配置方式

分析源码中的认证机制：
1. 检查 `src/services/auth/` 目录
2. 检查环境变量读取
3. 确定 API key 配置方式

---

## 任务 2: 创建配置文件模板

创建 `.env.example` 或配置说明：
```bash
# API 配置
ANTHROPIC_API_KEY=your-api-key-here
```

---

## 任务 3: 测试基本工具调用

配置 API 后测试：
```bash
# 简单提示测试
echo "list files in current directory" | ANTHROPIC_API_KEY=xxx bun run dev -p
```

---

## 任务 4: 更新文档

更新 README.md 添加：
- API 配置说明
- 使用示例
- 已知限制

---

## 执行命令

完成本计划后，执行：

```
/loop 30min 完成所有的v2.0.6_plan.md
```
# v2.0.5_plan.md - 功能测试与交互验证

## 前倾回顾

### v2.0.3/v2.0.4 完成情况

| 成果 | 状态 |
|------|------|
| 创建 30+ 缺失模块 stub | ✅ |
| 安装 Bun 运行时 v1.3.11 | ✅ |
| 程序可运行 (`bun run dev --version`) | ✅ |
| 修复 MACRO/feature() polyfill | ✅ |
| 修复 OpenTelemetry 兼容性 | ✅ |

### 当前状态

| 指标 | 值 |
|------|------|
| 编译错误 | 2175 (不影响运行) |
| 运行状态 | ✅ 可执行 |
| 版本输出 | `0.0.1-learn (Claude Code)` |

### v2.0.4 分析

程序已可以启动，但尚未测试交互功能。主要修复：
- `@anthropic-ai/sandbox-runtime` 包 stub
- `WorkflowTool` 相关文件
- `ink/global.ts` 运行时兼容
- OpenTelemetry `resourceFromAttributes` 兼容
- `MACRO` 和 `feature()` polyfill

---

## v2.0.5 目标

**目标**: 验证基本交互功能，测试核心命令

---

## 任务 1: 测试基本命令

```bash
# 测试帮助命令
bun run dev --help

# 测试版本
bun run dev --version
```

---

## 任务 2: 测试管道模式

```bash
# 测试管道输入
echo "say hello" | bun run dev -p
```

---

## 任务 3: 分析交互模式问题

如果交互模式不工作，分析原因：
1. 检查终端 TTY 检测
2. 检查 Ink 渲染器初始化
3. 检查 React 组件挂载

---

## 任务 4: 更新 README

更新 README.md 添加：
- 运行状态
- 已测试的功能
- 已知问题

---

## 执行命令

完成本计划后，执行：

```
/loop 30min 完成所有的v2.0.5_plan.md
```

# v2.0.3_plan.md - 继续减少编译错误

## 前倾回顾

### v2.0.2 完成情况

| 成果 | 状态 |
|------|------|
| 编译错误从 2271 降至 2034 | ✅ |
| 替换 message.ts 类型定义 | ✅ |
| 创建 diff.d.ts 类型声明 | ✅ |
| 添加 tools.ts 缺失类型 | ✅ |

### 当前编译错误: 2034

### 主要错误分布

| 文件 | 错误数 | 说明 |
|------|--------|------|
| src/utils/messages.ts | 106 | 消息处理工具 |
| src/screens/REPL.tsx | 105 | 主界面组件 |
| src/utils/hooks.ts | 85 | React hooks |
| src/main.tsx | 51 | 入口文件 |
| src/components/PromptInput/ | 47 | 用户输入组件 |
| src/QueryEngine.ts | 43 | 查询引擎 |

### 缺失模块分析

从错误信息识别的缺失模块:
- `../../assistant/index.js`
- `../../bridge/peerSessions.js`
- `../../coordinator/workerAgent.js`
- `../../proactive/index.js`
- `../../services/compact/reactiveCompact.js`
- `../../services/contextCollapse/index.js`
- `../../services/lsp/types.js`
- `../../services/skillSearch/*.js`
- `../../skills/mcpSkills.js`

---

## v2.0.3 目标

**目标**: 将编译错误从 2034 降至 < 1500

---

## 任务 1: 创建缺失模块 stub

### 1.1 创建 services 目录结构

```
src/services/
├── assistant/
│   └── index.ts
├── bridge/
│   └── peerSessions.ts
├── coordinator/
│   └── workerAgent.ts
├── proactive/
│   └── index.ts
├── compact/
│   └── reactiveCompact.ts (已有 snipCompact, snipProjection)
├── contextCollapse/
│   ├── index.ts
│   └── operations.ts
├── lsp/
│   └── types.ts
├── skillSearch/
│   ├── featureCheck.ts
│   ├── remoteSkillLoader.ts
│   ├── remoteSkillState.ts
│   └── telemetry.ts
└── skills/
    └── mcpSkills.ts
```

### 1.2 创建工具模块 stub

```
src/tools/
├── MonitorTool/
│   └── MonitorTool.ts
├── OverflowTestTool/
│   └── OverflowTestTool.ts
└── tasks/
    └── MonitorMcpTask/
        └── MonitorMcpTask.ts
```

---

## 任务 2: 修复 messages.ts 错误

分析 106 个错误的根本原因，可能需要:
1. 添加缺失的类型导出
2. 修复类型断言问题

---

## 任务 3: 尝试运行

当错误降至 1500 以下时，尝试:
```bash
bun run dev
```

---

## 执行命令

完成本计划后，执行：

```
/loop 20min 完成所有的v2.0.3_plan.md
```

# v2.0.7_plan.md - 代码清理与优化

## 前倾回顾

### v2.0.6 完成情况

| 成果 | 状态 |
|------|------|
| 检查 API 配置方式 | ✅ 环境变量 ANTHROPIC_API_KEY |
| 创建 .env.example | ✅ 已创建 |
| 更新 README.md | ✅ 添加配置说明 |

### 当前状态

| 指标 | 值 |
|------|------|
| 编译错误 | 2175 |
| 运行状态 | ✅ CLI 正常 |
| 文档完整性 | ✅ 完整 |

### v2.0.6 分析

所有配置和文档任务已完成。用户现在可以：
1. 配置 ANTHROPIC_API_KEY 环境变量
2. 使用管道模式进行测试

---

## v2.0.7 目标

**目标**: 代码清理，减少编译错误，提高代码质量

---

## 任务 1: 分析主要错误源

```bash
npx tsc --noEmit 2>&1 | grep "error TS" | sed 's/(.*//' | sort | uniq -c | sort -rn | head -20
```

---

## 任务 2: 修复高频错误

针对错误最多的文件进行修复：
1. `src/utils/messages.ts` (~106 错误)
2. `src/screens/REPL.tsx` (~105 错误)
3. `src/utils/hooks.ts` (~85 错误)

---

## 任务 3: 清理无用代码

1. 移除未使用的导入
2. 修复类型断言问题
3. 添加缺失的类型声明

---

## 任务 4: 更新 CHANGELOG 和提交

---

## 执行命令

完成本计划后，执行：

```
/loop 30min 完成所有的v2.0.7_plan.md
```

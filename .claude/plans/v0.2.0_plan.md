# v0.2.0_plan.md - Claude Code 源码学习计划

## 前倾回顾

### v0.1.0 完成情况

| 目标 | 状态 | 成果 |
|------|------|------|
| 补全缺失类型定义 | ✅ 完成 | message.ts, tools.ts, utils.ts |
| Bun API Polyfill | ✅ 完成 | bun.ts (hash, which, YAML, semver, spawn...) |
| 权限系统分析 | ✅ 完成 | permission-system.md 文档 |

### v0.1.0 关键发现

1. **类型系统可重建**: 通过 import 分析成功重建缺失类型
2. **Bun API 可模拟**: 大部分 Bun 特有 API 可用 Node.js 替代
3. **权限系统复杂**: 多层规则来源 + 分类器集成

### v0.1.0 遗留问题

- 编译错误仍多 (缺失更多类型文件)
- MACRO 宏未定义
- 部分模块循环依赖

---

## v0.2.0 实验目标

### 目标 1: MCP 协议集成分析
- [ ] 分析 `services/mcp/` 目录结构
- [ ] 理解 MCP 客户端连接流程
- [ ] 记录 MCP 工具调用机制

### 目标 2: Compact 上下文压缩分析
- [ ] 分析 `services/compact/` 目录
- [ ] 理解 auto-compact 触发条件
- [ ] 记录 compact 算法策略

### 目标 3: Agent 子进程管理分析
- [ ] 分析 `tools/AgentTool/` 目录
- [ ] 理解子 agent 生命周期
- [ ] 记录 agent 通信协议

### 目标 4: 再次运行编译测试
- [ ] 安装新增依赖 (yaml, semver, which, string-width, wrap-ansi)
- [ ] 运行 tsc --noEmit
- [ ] 统计剩余错误数量

---

## 执行命令

完成本计划所有目标后，执行：

```
/loop 20min 完成所有的v0.2.0_plan.md
```

# v2.3.12_plan.md - Shim 包 + Preload 架构优化

## 背景

参考 `claude-code-rebuilt` 项目，采用 Shim 包 + Preload 方案彻底解决编译问题。

### 当前状态 vs 目标

| 指标 | 当前 | 目标 |
|------|------|------|
| 编译错误 | 461 | 0 |
| 方案 | .d.ts 类型声明 | Shim 包 + Preload |
| 运行时支持 | ❌ 部分不可用 | ✅ 完全可用 |

---

## 方案对比

| 特性 | .d.ts 方案 | Shim + Preload |
|------|-----------|----------------|
| 解决编译错误 | 部分 | 完全 |
| 运行时可用 | ❌ | ✅ |
| bun:bundle 支持 | ❌ | ✅ |
| 复杂度 | 中 | 低 |
| 维护成本 | 高 | 低 |

---

## 实施计划

### Phase 1: 创建基础架构

**时间估计: 30分钟**

#### 1.1 创建目录结构

```
src/_external/
├── preload.ts          # Bun preload 脚本
├── shims/
│   ├── @anthropic-ai/
│   │   ├── sandbox-runtime/
│   │   │   ├── package.json
│   │   │   └── index.js
│   │   ├── mcpb/
│   │   ├── claude-agent-sdk/
│   │   ├── vertex-sdk/
│   │   └── foundry-sdk/
│   ├── @ant/
│   │   ├── claude-for-chrome-mcp/
│   │   ├── computer-use-input/
│   │   ├── computer-use-mcp/
│   │   └── computer-use-swift/
│   ├── color-diff-napi/
│   ├── image-processor-napi/
│   ├── audio-capture-napi/
│   ├── modifiers-napi/
│   └── url-handler-napi/
```

#### 1.2 创建 bunfig.toml

```toml
preload = ["./src/_external/preload.ts"]

[loader]
".md" = "text"
```

#### 1.3 创建 preload.ts

```typescript
import { plugin } from "bun";

const version = process.env.CLI_VERSION || "1.0.0-likecode";

(globalThis as any).MACRO = {
  VERSION: version,
  BUILD_TIME: new Date().toISOString(),
  PACKAGE_URL: "https://github.com/likecode/likecode",
  NATIVE_PACKAGE_URL: "",
  VERSION_CHANGELOG: "",
  FEEDBACK_CHANNEL: "",
  ISSUES_EXPLAINER: "",
};

plugin({
  name: "bun-bundle-shim",
  setup(build) {
    build.module("bun:bundle", () => ({
      exports: { feature: (_name: string): boolean => false },
      loader: "object",
    }));
  },
});
```

---

### Phase 2: 创建 Shim 包

**时间估计: 1小时**

#### 2.1 @anthropic-ai/sandbox-runtime

```javascript
// index.js
'use strict'

class SandboxViolationStore {
  subscribe(listener) { listener([]); return () => {} }
  getTotalCount() { return 0 }
}

const SandboxRuntimeConfigSchema = {
  parse: (c) => c,
  safeParse: (c) => ({ success: true, data: c })
}

class SandboxManager {
  static checkDependencies() { return { errors: [], warnings: [] } }
  static isSupportedPlatform() { return false }
  static wrapWithSandbox(cmd) { return Promise.resolve(cmd) }
  static initialize() { return Promise.resolve() }
  static updateConfig() {}
  static reset() { return Promise.resolve() }
  static getFsReadConfig() { return undefined }
  static getFsWriteConfig() { return undefined }
  static getNetworkRestrictionConfig() { return undefined }
  static getSandboxViolationStore() { return new SandboxViolationStore() }
}

module.exports = { SandboxManager, SandboxRuntimeConfigSchema, SandboxViolationStore }
```

#### 2.2 @anthropic-ai/mcpb

```javascript
// index.js
'use strict'
module.exports = {
  encode: (data) => Buffer.from(JSON.stringify(data)),
  decode: (data) => JSON.parse(data.toString())
}
```

#### 2.3 @anthropic-ai/claude-agent-sdk

```javascript
// index.js
'use strict'
module.exports = {
  createAgent: () => ({}),
  runAgent: () => Promise.resolve({})
}
```

#### 2.4 color-diff-napi

```javascript
// index.js
'use strict'
class ColorDiff {}
class ColorFile {}
function getSyntaxTheme() { return null }
module.exports = { ColorDiff, ColorFile, getSyntaxTheme }
```

#### 2.5 image-processor-napi

```javascript
// index.js
'use strict'
module.exports = {
  processImage: () => Promise.resolve(null)
}
```

#### 2.6 audio-capture-napi

```javascript
// index.js
'use strict'
module.exports = {
  startCapture: () => {},
  stopCapture: () => {}
}
```

#### 2.7 @ant/* 模块

```javascript
// 各 @ant 模块 index.js
'use strict'
module.exports = {}
```

---

### Phase 3: 更新 package.json

**时间估计: 15分钟**

```json
{
  "dependencies": {
    "@anthropic-ai/sandbox-runtime": "file:./src/_external/shims/@anthropic-ai/sandbox-runtime",
    "@anthropic-ai/mcpb": "file:./src/_external/shims/@anthropic-ai/mcpb",
    "@anthropic-ai/claude-agent-sdk": "file:./src/_external/shims/@anthropic-ai/claude-agent-sdk",
    "@anthropic-ai/vertex-sdk": "file:./src/_external/shims/@anthropic-ai/vertex-sdk",
    "@anthropic-ai/foundry-sdk": "file:./src/_external/shims/@anthropic-ai/foundry-sdk",
    "@ant/claude-for-chrome-mcp": "file:./src/_external/shims/@ant/claude-for-chrome-mcp",
    "@ant/computer-use-input": "file:./src/_external/shims/@ant/computer-use-input",
    "@ant/computer-use-mcp": "file:./src/_external/shims/@ant/computer-use-mcp",
    "@ant/computer-use-swift": "file:./src/_external/shims/@ant/computer-use-swift",
    "color-diff-napi": "file:./src/_external/shims/color-diff-napi",
    "image-processor-napi": "file:./src/_external/shims/image-processor-napi",
    "audio-capture-napi": "file:./src/_external/shims/audio-capture-napi",
    "modifiers-napi": "file:./src/_external/shims/modifiers-napi",
    "url-handler-napi": "file:./src/_external/shims/url-handler-napi"
  }
}
```

---

### Phase 4: 移除冗余 .d.ts

**时间估计: 15分钟**

删除不再需要的类型声明文件：
- `src/types/external-modules.d.ts` 中的 shim 模块声明

---

### Phase 5: 验证

**时间估计: 15分钟**

```bash
# 1. 安装依赖
bun install

# 2. 类型检查
bun run typecheck

# 3. 运行测试
bun run src/entrypoints/cli.tsx --version
bun run src/entrypoints/cli.tsx --help

# 4. 构建测试
bun run build
```

---

## 任务清单

### Phase 1: 基础架构
- [ ] 创建 `src/_external/` 目录结构
- [ ] 创建 `bunfig.toml`
- [ ] 创建 `preload.ts`

### Phase 2: Shim 包
- [ ] 创建 `@anthropic-ai/sandbox-runtime` shim
- [ ] 创建 `@anthropic-ai/mcpb` shim
- [ ] 创建 `@anthropic-ai/claude-agent-sdk` shim
- [ ] 创建 `@anthropic-ai/vertex-sdk` shim
- [ ] 创建 `@anthropic-ai/foundry-sdk` shim
- [ ] 创建 `@ant/*` 系列 shim (4个)
- [ ] 创建 `color-diff-napi` shim
- [ ] 创建 `image-processor-napi` shim
- [ ] 创建 `audio-capture-napi` shim
- [ ] 创建 `modifiers-napi` shim
- [ ] 创建 `url-handler-napi` shim

### Phase 3: 配置更新
- [ ] 更新 `package.json` 依赖
- [ ] 运行 `bun install`

### Phase 4: 清理
- [ ] 移除冗余的 `.d.ts` 声明
- [ ] 更新 `tsconfig.json` (如需要)

### Phase 5: 验证
- [ ] 类型检查通过 (0 errors)
- [ ] `--version` 正常
- [ ] `--help` 正常
- [ ] 构建成功

---

## 预期结果

| 指标 | 结果 |
|------|------|
| 编译错误 | **0** |
| 运行时支持 | ✅ 完全可用 |
| bun:bundle | ✅ 通过 preload 注入 |
| 维护复杂度 | ✅ 低 |

---

## 风险

1. **Shim 实现不完整** - 需要确保所有被调用的方法都有 stub
2. **路径问题** - 确保 `file:` 协议路径正确
3. **类型推断** - Shim 使用 `.js` 可能丢失类型，可添加 `.d.ts` 补充

---

## 开始时间

**2026-04-03 12:00 CST**

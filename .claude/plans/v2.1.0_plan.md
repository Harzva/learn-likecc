# v2.1.0_plan.md - 功能测试与编译错误减少

## 前倾回顾

### v2.0.9 完成情况

| 成果 | 状态 |
|------|------|
| GitHub Release v2.0.7 | ✅ 已发布 |
| 课程详情页 s01 | ✅ 已创建 |
| CHANGELOG 更新 | ✅ 已更新 |

### 当前状态

| 指标 | 值 |
|------|------|
| 编译错误 | 2180 |
| 运行状态 | ✅ CLI 正常 |
| Release | v2.0.7 |
| 课程详情页 | S01 已完成 |

### v2.0.9 分析

课程网站基础架构已建立，S01 详情页已创建。

---

## v2.1.0 目标

**目标**: 减少编译错误，测试 API 调用

---

## 任务 1: 分析主要错误源

```bash
npx tsc --noEmit 2>&1 | grep "error TS" | sed 's/(.*//' | sort | uniq -c | sort -rn | head -10
```

---

## 任务 2: 修复高频错误文件

目标文件:
- `src/utils/messages.ts` (~106 错误)
- `src/screens/REPL.tsx` (~105 错误)
- `src/utils/hooks.ts` (~85 错误)

---

## 任务 3: 测试 API 调用

使用 ANTHROPIC_API_KEY 测试管道模式:
```bash
echo "say hello" | ANTHROPIC_API_KEY=xxx bun run dev -p
```

---

## 任务 4: 更新 CHANGELOG 和提交

---

## 执行命令

完成本计划后，执行：

```
/loop 30min 完成所有的v2.1.0_plan.md
```
# v2.0.0_plan.md - 源码恢复与可运行版本

## 前倾回顾

### v1.0.0 完成情况

| 成果 | 状态 |
|------|------|
| 12 章节课程文档 | ✅ |
| 12 个示例代码 | ✅ |
| 源码分析文档 | ✅ |

### 当前源码状态

- **编译错误**: 6099 个
- **缺失配置**: package.json, tsconfig.json, bunfig.toml
- **缺失类型**: SDK 类型、消息类型不完整
- **Feature flags**: 部分实现

### reference 项目学习

从 `reference/` 目录的逆向工程项目学到：

1. **open-claude-code**: 完整的编译修复流程
2. **claude-code2 (CCB)**: 详细的恢复步骤和 TODO 管理
3. **leaked-claude-code**: 架构说明和 feature flag 管理

---

## v2.0.0 目标: 恢复可运行版本

### 目标 1: 依赖重建 ✅ (部分)

- [x] 更新 package.json 添加 workspaces
- [x] 更新 tsconfig.json 添加 bun types
- [x] 安装 @types/bun
- [ ] 创建 bunfig.toml (Bun 配置)

### 目标 2: Stub 包创建 ✅

已从 `reference/claude-code2/packages/` 复制：

- [x] `packages/color-diff-napi/` - 颜色差异
- [x] `packages/modifiers-napi/` - 修饰键检测
- [x] `packages/audio-capture-napi/` - 音频捕获
- [x] `packages/image-processor-napi/` - 图像处理
- [x] `packages/url-handler-napi/` - URL 处理
- [x] `packages/@ant/computer-use-mcp/`
- [x] `packages/@ant/computer-use-input/`
- [x] `packages/@ant/computer-use-swift/`
- [x] `packages/@ant/claude-for-chrome-mcp/`

### 目标 3: SDK 类型补全 ✅ (部分)

已复制 SDK 类型文件：
- [x] `coreTypes.generated.ts`
- [x] `runtimeTypes.ts`
- [x] `toolTypes.ts`
- [x] `settingsTypes.generated.ts`
- [x] `sdkUtilityTypes.ts`
- [x] `controlTypes.ts`

### 目标 4: Feature Flag 完善

- [x] 已有 `bun:bundle` polyfill
- [ ] 创建 feature flag 配置文件
- [ ] 默认关闭所有高级 feature flags

### 目标 5: 编译修复 (进行中)

| 指标 | 之前 | 当前 | 目标 |
|------|------|------|------|
| 编译错误 | 6099 | 6087 | < 100 |

---

## 执行策略

### 阶段 1: 配置重建 (优先级最高)

```bash
# 1. 创建 package.json (参考 claude-code2)
# 2. 创建 tsconfig.json
# 3. 运行 bun install
```

### 阶段 2: Stub 包创建

```bash
# 创建 packages/ 目录结构
# 每个包创建最小 stub 实现
```

### 阶段 3: 类型修复

```bash
# 补全缺失的类型定义
# 修复 import 错误
```

### 阶段 4: 验证

```bash
# 运行 bun run dev
# 检查启动日志
```

---

## 参考资源

| 文件 | 用途 |
|------|------|
| `reference/claude-code2/package.json` | 依赖配置参考 |
| `reference/claude-code2/tsconfig.json` | TS 配置参考 |
| `reference/claude-code2/packages/` | Stub 包实现参考 |
| `reference/claude-code2/CLAUDE.md` | 架构说明 |

---

## 成功标准

- [ ] `bun install` 成功
- [ ] `npx tsc --noEmit` 错误 < 100
- [ ] `bun run src/entrypoints/cli.tsx` 可启动
- [ ] 显示版本号并进入交互模式

---

## 执行命令

完成本计划后，执行：

```
/loop 20min 完成所有的v2.0.0_plan.md
```
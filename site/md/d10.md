# D10: Hooks Extension 深挖 - Claude Code Course

> **在线页面**: https://harzva.github.io/learn-likecc/d10.html  
> **本文件**: `site/md/d10.md`  
> **说明**: 正文可与 HTML 逐步对齐；当前版本以概要为主。

## 概要

基于本仓库 `ccsource/claude-code-main` 的 **Hooks** 走读：生命周期矩阵、并行与权限合并、「最后一条改参获胜」、源码摘录与超时/信任门。含较长走读顺序与多道自测参考答案（参数覆盖、Post-hook 自动 approve、并行 allow/ask/deny、审计链）。

## 正文（可选 / 待补全）

# D10: Hooks Extension 深挖 - Claude Code Course

> **双版本阅读**
> - **网页版（排版 / 主题 / Mermaid）**: [https://harzva.github.io/learn-likecc/d10.html](https://harzva.github.io/learn-likecc/d10.html) — 当前浏览器所见即此版。
> - **Markdown 版（PR / 离线 / diff）**: 本仓库 [`site/md/d10.md`](https://github.com/Harzva/learn-likecc/blob/main/site/md/d10.md) — 在 GitHub 上打开或拉取后本地编辑。

## 目录（对照 HTML）

- 🔬 深挖目标
- 📂 源码锚点（本仓库 ccsource/claude-code-main ）
- 🪝 生命周期矩阵
- ⚡ 并行、权限合并与「最后一条改参获胜」
- 📎 源码摘录（ ccsource/.../hooks.ts ）
- 🧪 超时、信任门与环境开关
- 📖 走读顺序（带搜索关键词）
- ✏️ 自测 1 · 参考答案：两个 hook 改同一参数，谁说了算？
- ✏️ 自测 2 · 参考答案：Post-hook 能「自动 approve」吗？
- ✏️ 自测 3 · 参考答案：并行 hook 的 allow / ask / deny 谁赢？
- ✏️ 自测 4 · 参考答案：Post 改写输出与审计链
- ✏️ 自测（题干回顾）

## 各节摘要

### 🔬 深挖目标

Hooks 把固定流程变成可插拔管道。本讲要求你能对照 真实调度代码 回答： 事件从哪进、匹配规则、并行还是串行、权限/改参如何合并、超时与信任门、失败是否阻断主流程 ——而不是只背事件名。

### 📂 源码锚点（本仓库 ccsource/claude-code-main ）

克隆课程仓库且已包含 ccsource 时，按下表打开文件即可；行号随上游变动可能漂移，以符号名搜索为准。

### 🪝 生命周期矩阵

（详见网页版该节。）

### ⚡ 并行、权限合并与「最后一条改参获胜」

与「hook 按注册顺序串行改同一个字段」的直觉不同，上游对 一批匹配的 command/prompt/… hook 采用 并行启动 + 聚合结果 ： matchingHooks.map(…) 后为 for await (const result of all(hookPromises)) 。权限类结果有明确优先级： deny &gt; ask &gt; allow （见注释 Apply precedence rules ）。

### 📎 源码摘录（ ccsource/.../hooks.ts ）

下列片段与当前课程随附的 ccsource/claude-code-main 一致，便于你全文搜索时核对；上游更新后请以本地文件为准。

### 🧪 超时、信任门与环境开关

（详见网页版该节。）

### 📖 走读顺序（带搜索关键词）

（详见网页版该节。）

### ✏️ 自测 1 · 参考答案：两个 hook 改同一参数，谁说了算？

配置了两个 PreToolUse hook，都对 Bash 生效，且都在 JSON 里返回 hookSpecificOutput.updatedInput 修改同一字段（例如 command ）。最终进入工具执行的到底是哪一份？用户问「优先级」时你怎么解释？

### ✏️ 自测 2 · 参考答案：Post-hook 能「自动 approve」吗？

能否靠 PostToolUse hook 实现「某类工具一律自动批准」？安全代价是什么？

### ✏️ 自测 3 · 参考答案：并行 hook 的 allow / ask / deny 谁赢？

同一 PreToolUse 、同一工具，hook A 返回 allow，hook B 返回 ask，hook C 返回 deny。最终权限行为是什么？

### ✏️ 自测 4 · 参考答案：Post 改写输出与审计链

PostToolUse hook 若替换 MCP 工具返回体（源码中的 updatedMCPToolOutput ），审计日志应记录「原始结果」还是「改写后结果」？模型看到的与落盘的一致吗？

### ✏️ 自测（题干回顾）

（详见网页版该节。）

---

*正文与图表以网页版为准；本 MD 为讲义索引与审阅用草稿。*

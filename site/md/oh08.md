# OH08: 后台任务（对标 S08）

> **在线**: `/oh08.html` · **对照** [S08](s08.md)  
> **上游**: `tasks/`（`BackgroundTaskManager`、`get_task_manager`、`spawn_local_agent_task`、`spawn_shell_task`）、`tools/task_*_tool.py`（create/list/get/stop/output/update）

## 要点

- 任务与 Agent Loop **并行**：长耗时 Shell/子 Agent 可走后台，`task_output` 等拉取结果。  
- `CommandRegistry` / TUI 侧常通过 `get_task_manager()` 与任务状态同步（可继续追 `registry.py` 引用）。

# OH05: 上下文压缩（对标 S05）

> **在线**: `/oh05.html` · **对照** [S05](s05.md)  
> **上游**: `services/compact/`（microcompact、LLM 摘要、`auto_compact_if_needed`）、`prompts/context.py`（`build_runtime_system_prompt` 拼 CLAUDE.md / memory / skills）

## 要点

- 模块注释写明与 Claude Code compaction 思路对齐：`COMPACTABLE_TOOLS`、token 阈值、`auto_compact_if_needed` 在 `query.run_query` 每轮开头调用。  
- **系统提示**组装与「压缩后仍要给模型什么背景」同题：`build_runtime_system_prompt` 拉 skills、memory、`CLAUDE.md`。

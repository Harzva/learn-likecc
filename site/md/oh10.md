# OH10: Hooks（对标 S10）

> **在线**: `/oh10.html` · **对照** [S10](s10.md)  
> **上游**: `hooks/events.py`（`HookEvent`：`SESSION_START`/`END`、`PRE_TOOL_USE`、`POST_TOOL_USE`）、`hooks/executor.py`、`hooks/loader.py`（`load_hook_registry`）

## 要点

- **调用点**：`query._execute_tool_call` 在权限通过后、execute 前后调用 `hook_executor.execute`。  
- 返回 `blocked` 时直接错误型 `ToolResultBlock`，与课内 PreToolUse 阻断一致。

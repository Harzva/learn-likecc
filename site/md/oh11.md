# OH11: Vim / 键位（对标 S11）

> **在线**: `/oh11.html` · **对照** [S11](s11.md)  
> **上游**: `vim/transitions.py`（`toggle_vim_mode` 极简状态）、`keybindings/`（`default_bindings.py`、`loader.py`、`resolver.py`、`parser.py`）

## 要点

- OpenHarness **Vim 层较薄**：状态切换 + 可配置键位解析；重交互在 prompt_toolkit / TUI 主循环。  
- 读 `keybindings/` 可看「命令→动作」如何与 S11 课内 Vim 模式对照。

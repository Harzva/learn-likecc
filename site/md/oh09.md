# OH09: Bridge / 前端宿主（对标 S09）

> **在线**: `/oh09.html` · **对照** [S09](s09.md)  
> **上游**: `bridge/`（`BridgeSessionManager`、`spawn_session`、`WorkSecret`）、`ui/backend_host.py`（`OHJSON:` 协议、与 React TUI 的 JSON-lines）、`ui/react_launcher.py`

## 要点

- **外接 UI**：`BackendHost` 把 `StreamEvent` 打成前端可消费事件；与纯终端 TUI 同引擎、不同宿主。  
- Bridge 会话与 `encode_work_secret` 等与远程协作场景相关，和课内 Bridge 叙事对照。

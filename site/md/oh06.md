# OH06: Subagent / 多 Agent（对标 S06）

> **在线**: `/oh06.html` · **对照** [S06](s06.md)  
> **上游**: `coordinator/`（agent 定义、team registry）、`swarm/`（后端 registry、`TeammateSpawnConfig`）、`tools/agent_tool.py`（`agent` 工具：`local_agent` / `remote_agent` / `in_process_teammate`）

## 要点

- **委派**：模型通过 `agent` tool spawn 子任务；实现上结合 `get_agent_definition` 与 `get_backend_registry()` 选 in-process 或子进程。  
- 与课内 worktree/remote 隔离叙事对照：OH 用 **mode + swarm backend** 表达类似产品分叉。

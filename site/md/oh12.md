# OH12: Git / Worktree（对标 S12）

> **在线**: `/oh12.html` · **对照** [S12](s12.md)  
> **上游**: `tools/enter_worktree_tool.py`、`tools/exit_worktree_tool.py`、`tools/bash_tool.py`（一般 Git 命令仍走 Shell）

## 要点

- **enter_worktree**：`git worktree add` 封装，失败返回 `ToolResult` 错误。  
- 与 S06 隔离叙事衔接：worktree 换根目录后 `cwd` 与权限路径解析均在 `ToolExecutionContext` 上体现。

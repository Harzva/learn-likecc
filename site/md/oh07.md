# OH07: MCP（对标 S07）

> **在线**: `/oh07.html` · **对照** [S07](s07.md)  
> **上游**: `mcp/`（`McpClientManager`、配置加载）、`tools/mcp_tool.py`（`McpToolAdapter`：动态 `mcp__server__tool` 名、JSON Schema→Pydantic）、`read_mcp_resource_tool.py` / `list_mcp_resources_tool.py`

## 要点

- MCP 工具在 `create_default_tool_registry(mcp_manager)` 时批量注册为普通 `BaseTool`，Loop 无需区分内置与 MCP。  
- `_input_model_from_schema` 用 `create_model` 把远端 schema 变成可 `model_validate` 的模型。

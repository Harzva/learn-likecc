# S12: Git Integration - Claude Code Course

> **在线页面**: https://harzva.github.io/learn-likecc/s12.html  
> **本文件**: `site/md/s12.md`  
> **说明**: 正文可与 HTML 逐步对齐；当前版本以概要为主。

## 概要

本讲讨论 **Git 集成**：在 Agent 工作流中查看 diff、暂存与提交相关能力的设计意图与安全边界。

## 目录（对照 HTML）

章节标题与 [`site/s12.html`](../s12.html) 内 `h2` 对应。**Git 工具卡片、`createCommit` 代码块、GitHub 能力表与结课引导见在线页。**

### 🎯 问题

AI 编程需要读历史、看差异、安全改代码并 eventually 与远端协作；这些能力要封装在可控 API 后而非任意 shell。

### 💡 解决方案

用 Git Utils 聚合对 `git` CLI 的调用：status、diff、log、worktree 等只读/受控写路径，再暴露给模型与 UI。

### 🔑 Git 封装功能

分卡片说明 Status、Diff、Log、Worktree 等入口各自解决的开发任务。图标与一句描述见在线页。

### 🔧 /commit 流程

逐步展示从取 status/diff、可选生成 message、到 `git add` 与 `commit` 的 happy path。完整异步函数示例见在线页。

### 🐙 GitHub 集成

列举 `gh` CLI、PR、Issue、Release 等远程协作能力在课程语境下的定位。选项表见在线页。

### 🎉 主线最后一讲

收束 S01–S12，提示 24 讲路线中剩余 D 线深挖（尤其 D12），并链到对应页面。

## 正文（可选 / 待补全）

代码与表格见在线页；权限与 workspace 边界见 **D12 深挖** 参考答案。
